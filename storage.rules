rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow authenticated users to read and write storage.
      // TODO: Add proper security rules before production.
      allow read, write: if request.auth != null;
    }
  }
}
