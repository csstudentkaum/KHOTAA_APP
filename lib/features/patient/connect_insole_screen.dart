import 'package:flutter/material.dart';

/// Connect insole screen
/// TODO: Implement connect insole screen
class ConnectInsoleScreen extends StatelessWidget {
  const ConnectInsoleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Connect Insole Screen')));
  }
}
